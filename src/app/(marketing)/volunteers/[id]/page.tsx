import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { Award, CalendarCheck2, MapPin, Medal } from "lucide-react";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import { EmptyState } from "@/components/shared/empty-state";
import { createClient } from "@/lib/supabase/server";
import { formatDate, initials } from "@/lib/utils";

export async function generateMetadata({ params }: { params: Promise<{ id: string }> }): Promise<Metadata> { const { id } = await params; const supabase = await createClient(); const { data } = await supabase.from("public_volunteer_profiles").select("full_name, bio").eq("id", id).maybeSingle(); return data ? { title: data.full_name ?? "Волонтёр", description: data.bio ?? "Публичный профиль волонтёра Qamqor" } : { title: "Волонтёр не найден" }; }

export default async function PublicVolunteerPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const supabase = await createClient();
  const [{ data: profile }, { data: projects }, { data: awarded }] = await Promise.all([
    supabase.from("public_volunteer_profiles").select("*").eq("id", id).maybeSingle(),
    supabase.from("public_volunteer_projects").select("*").eq("volunteer_id", id).order("end_date", { ascending: false }),
    supabase.from("volunteer_achievements").select("awarded_at, achievements(*)").eq("volunteer_id", id).order("awarded_at", { ascending: false }),
  ]);
  if (!profile) notFound();
  return <section className="py-14 sm:py-18"><div className="page-shell"><div className="overflow-hidden rounded-[2rem] border bg-surface"><div className="hero-grid h-36 bg-primary/10 sm:h-44" /><div className="px-6 pb-8 sm:px-10"><Avatar className="-mt-14 size-28 border-4 border-surface shadow-xl"><AvatarImage src={profile.avatar_url ?? undefined} alt={profile.full_name ?? "Волонтёр"} /><AvatarFallback className="text-2xl">{initials(profile.full_name)}</AvatarFallback></Avatar><div className="mt-5 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><Badge variant="secondary">Волонтёр Qamqor</Badge><h1 className="mt-3 text-3xl font-black tracking-tight sm:text-4xl">{profile.full_name}</h1><p className="mt-2 flex items-center gap-2 text-sm text-muted-foreground"><MapPin className="size-4 text-primary" />{profile.city ?? "Город не указан"}</p></div><div className="flex gap-7"><div><p className="text-3xl font-black text-success-foreground">{Number(profile.confirmed_hours ?? 0)}</p><p className="text-xs text-muted-foreground">часов</p></div><div><p className="text-3xl font-black text-success-foreground">{Number(profile.completed_projects ?? 0)}</p><p className="text-xs text-muted-foreground">проектов</p></div></div></div>{profile.bio ? <p className="mt-7 max-w-3xl text-base leading-8 text-muted-foreground">{profile.bio}</p> : null}</div></div><div className="mt-8 grid gap-8 lg:grid-cols-[1.25fr_.75fr]"><div><h2 className="text-2xl font-black">Завершённые проекты</h2>{projects?.length ? <div className="mt-5 grid gap-4">{projects.map((project) => <Card key={project.project_id}><CardContent className="flex items-center gap-4 p-5"><div className="flex size-12 shrink-0 items-center justify-center rounded-2xl bg-success/15 text-success-foreground"><CalendarCheck2 /></div><div className="min-w-0 flex-1"><p className="truncate font-black">{project.title}</p><p className="mt-1 text-xs text-muted-foreground">{project.city} · {formatDate(project.end_date!, "d MMMM yyyy")}</p></div><div className="text-right"><p className="font-black text-success-foreground">{project.hours} ч.</p><p className="text-xs text-muted-foreground">подтверждено</p></div></CardContent></Card>)}</div> : <div className="mt-5"><EmptyState title="История только начинается" description="Завершённые проекты появятся после подтверждения координатором." /></div>}</div><div><h2 className="text-2xl font-black">Достижения</h2>{awarded?.length ? <div className="mt-5 grid gap-4">{awarded.map((item) => <Card key={`${item.awarded_at}-${item.achievements?.id}`}><CardContent className="p-5"><div className="flex items-center gap-3"><div className="flex size-11 items-center justify-center rounded-2xl bg-accent/20 text-accent-foreground"><Medal /></div><div><p className="font-black">{item.achievements?.title}</p><p className="mt-1 text-xs text-muted-foreground">{item.achievements?.required_hours}+ часов</p></div></div><p className="mt-3 text-sm leading-6 text-muted-foreground">{item.achievements?.description}</p></CardContent></Card>)}</div> : <div className="mt-5"><EmptyState icon={Award} title="Награды впереди" description="Достижения выдаются автоматически за подтверждённые часы." /></div>}</div></div></div></section>;
}
